# ==========================================
# MathNote 환경 변수 설정
# ==========================================
# 이 파일을 복사하여 .env 파일을 생성하세요:
# cp .env.example .env
# ==========================================

# ==================== API Keys ====================
# OpenAI API 키 (필수)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Google API 키 (Gemini 사용 시)
GOOGLE_API_KEY=your-google-api-key-here

# ==================== AWS S3 설정 ====================
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_REGION=ap-northeast-2
S3_BUCKET_NAME=mathnote-bucket
S3_PRESIGNED_URL_EXPIRY=3600

# ==================== Processing Options ====================
# 프레임 추출 간격 (초) - 기본값: 1.0
FRAME_INTERVAL_SEC=1.0

# 슬라이드 전환 감지 임계값 (0.0~1.0) - 기본값: 0.85
SSIM_THRESHOLD=0.85

# 오디오 싱크 패딩 (초) - 기본값: 5.0
AUDIO_PADDING_SEC=5.0

# ==================== LLM Settings ====================
# LLM 제공자: "openai" 또는 "gemini"
LLM_PROVIDER=openai

# 텍스트 모델
LLM_MODEL=gpt-4o

# Vision 모델 (OCR용)
LLM_VISION_MODEL=gpt-4o

# 생성 온도 (0.0~1.0)
LLM_TEMPERATURE=0.3

# 최대 토큰 수
LLM_MAX_TOKENS=4096

# ==================== Whisper Settings ====================
# Whisper 모델: tiny, base, small, medium, large
WHISPER_MODEL=base

# ==================== CORS ====================
# 허용할 Origin (쉼표로 구분)
CORS_ORIGINS=["http://localhost:3000","http://localhost:5173"]
